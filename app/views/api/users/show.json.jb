{
  user: @user.id,
  username: @user.username,
  first_name: @user.first_name,
  last_name: @user.last_name,
  full_name: @user.first_name + " " + @user.last_name,
  journal_description: @user.journal_description,
}
